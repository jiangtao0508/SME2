# 数据与访问边界

本仓库只包含通用分析源码、文档和运行时生成的合成测试，不包含：

- 对方或第三方的源码；
- 从受访问控制环境带出的 MLIR、LLVM IR 或反汇编；
- 目标文件、共享库、测试日志或性能结果；
- 本地论文、实验输出和编译器安装文件。

分析器默认不会修改输入 dump，也不会复制完整源码或 IR。报告中仍可能包含：

- 测试命令和完整测试日志；
- 文件相对路径、大小和 SHA256；
- 操作与指令计数；
- FlagGems函数路径和PyTorch算子分发表；
- `readelf`、`nm` 和 `objdump` 的派生输出。

这些派生信息是否允许带离现场，必须服从对方的访问控制要求。无法确认时使用
`--no-package`，只在对方机器上查看结果，不复制报告包。

`prefetch_plugin/prefetch-snapshot` 会按命令行指定路径写出完整的 bufferized
函数 IR；Stage 1/Stage 2 还会生成修改后的 MLIR、LLIR、对象和反汇编。这些都
属于现场产物，必须只写入对方允许的项目输出目录，不得提交到本仓库或带离
平台。插件源码和合成测试本身不包含对方数据。
